.b-text-page__box
    h3.b-text-page__title.b-text-page__title_upline_yes
        | CHECK OUR SPECIAL OFFERS!

    .b-dedicated__description
        p
            | To make your experience with HOSTKEY even more exciting we are introducing ready to use Discounted Servers and Auction Servers.<br>The idea behind it is that the price depends on the stock availability. The more servers of a certain configuration we have — the more appealing the price gets. If you leave it for too long, the stock will go down and price will go up.<br><br> Be vigilant, don't miss your chance, others might beat you to it. Find your best deal.

._angular
    .b-container

        //-.dedicated-extra-price__box
            table.dedicated-extra-price__table.dedicated-extra-price__table_type_head
                tr.dedicated-extra-price__table-row
                    td.dedicated-extra-price__table-cell(style="width:300px;")

                    //-td.dedicated-extra-price__table-cell.js-checked
                        .dedicated-extra-price__table-title CPU number
                        label.b-checkbox-submit.b-checkbox-submit_size_70
                            span.b-checkbox-submit__text 1 CPU
                            input.js-checked-submit(name='check-01', type='radio')
                        label.b-checkbox-submit.b-checkbox-submit_size_70
                            span.b-checkbox-submit__text 2 CPU
                            input.js-checked-submit(name='check-01', type='radio')

                    td.dedicated-extra-price__table-cell
                    td.dedicated-extra-price__table-cell(style="width:370px;")
                        .dedicated-extra-price__table-title Sort
                        span(ui-select="filter.sort", options="list.sort", width="280", style="display:inline-block;")
                    //-td.dedicated-extra-price__table-cell
                        button.b-submit.dedicated-extra-price__table-submit(type='button') find

        .dedicated-extra-price__box

            table.dedicated-extra-price__table(ng-table="tableData", show-filter="true")
                thead
                    tr.dedicated-extra-price__table-row.dedicated-extra-price__table-row_title_yes
                        th.dedicated-extra-price__table-cell
                        th.dedicated-extra-price__table-cell.sortable(ng-class="{'sort-asc': tableData.isSortBy('CpuKpdCnt', 'asc'), 'sort-desc': tableData.isSortBy('CpuKpdCnt', 'desc')}", ng-click="tableData.sorting('CpuKpdCnt', tableData.isSortBy('CpuKpdCnt', 'asc') ? 'desc' : 'asc')", style="width:400px;")
                            div processor
                        th.dedicated-extra-price__table-cell.sortable(ng-class="{'sort-asc': tableData.isSortBy('Ram', 'asc'), 'sort-desc': tableData.isSortBy('Ram', 'desc')}", ng-click="tableData.sorting('Ram', tableData.isSortBy('Ram', 'asc') ? 'desc' : 'asc')")
                            div memory
                        th.dedicated-extra-price__table-cell.sortable(ng-class="{'sort-asc': tableData.isSortBy('Hdd', 'asc'), 'sort-desc': tableData.isSortBy('Hdd', 'desc')}", ng-click="tableData.sorting('Hdd', tableData.isSortBy('Hdd', 'asc') ? 'desc' : 'asc')", style="width:200px;")
                            div hard drive
                        th.dedicated-extra-price__table-cell.sortable(ng-class="{'sort-asc': tableData.isSortBy('Raid', 'asc'), 'sort-desc': tableData.isSortBy('Raid', 'desc')}", ng-click="tableData.sorting('Raid', tableData.isSortBy('Raid', 'asc') ? 'desc' : 'asc')", style="width:120px;")
                            div hw raid
                        th.dedicated-extra-price__table-cell.sortable(ng-class="{'sort-asc': tableData.isSortBy('Price', 'asc'), 'sort-desc': tableData.isSortBy('Price', 'desc')}", ng-click="tableData.sorting('Price', tableData.isSortBy('Price', 'asc') ? 'desc' : 'asc')", style="width:240px;")
                            div monthly
                        th.dedicated-extra-price__table-cell.dedicated-extra-price__table-cell_red_yes(style="width:315px;")
                            div price will change after:

                tbody(ng-repeat="s in $data")
                    tr.dedicated-extra-price__table-row
                        td.dedicated-extra-price__table-cell
                            span.b-icon.b-flag(ng-class="{'b-flag_country_nether': s.LocationCode==='NL'}")

                        td.dedicated-extra-price__table-cell(sortable="'CpuKpdCnt'", style="height: 60px;")
                            .dedicated-extra-price__text(ng-bind="s.CpuName")
                            kpd-indicator(cpu-kpd="s.CpuKpd", cpu-kpd-link="s.CpuKpdLink", cpu-cnt="s.CpuCnt")

                        td.dedicated-extra-price__table-cell(sortable="'Ram'")
                            span.dedicated-extra-price__text {{s.Ram}} GB

                        td.dedicated-extra-price__table-cell(sortable="'Hdd'")
                            span(ng-bind-html="s.Hdd")

                        td.dedicated-extra-price__table-cell(sortable="'Hdd'")
                            span.b-icon.dedicated-extra-price__icon-good(ng-if="s.Raid")

                        td.dedicated-extra-price__table-cell(sortable="'Price'")
                            .b-submit.dedicated-item-content__submit(ng-if="s.Price", href="", ng-click="selectSale(s)") {{s.Price|verboseCurrency:false}}

                        td.dedicated-extra-price__table-cell(style="position:relative;")
                            .black-sale__slider-item-timer(time-to="s.Timer", callback="changePrice(s)", style="position:absolute;top:0;")

                    tr.dedicated-extra-price__table-row(ng-if="s.Id === selectedSale.Id")
                        td.dedicated-extra-price__table-cell(colspan="9")
                            .b-dedicated__hide-block-close(ng-click="selectSale(s)")
                                span.b-icon.b-dedicated__hide-block-close-image
                                span.b-dedicated__hide-block-close-text hide

                            div(sale-server-calculator="", sale-server="s")

            .extra-links
                a.b-submit.extra-links-submit(href="/about/datacenter", target="_blank") About data centers
                a.b-submit.extra-links-submit(href="/support/speedtest", target="_blank") Speedtest
                a.b-submit.extra-links-submit(href="/about/contacts", target="_blank") Try before you buy
